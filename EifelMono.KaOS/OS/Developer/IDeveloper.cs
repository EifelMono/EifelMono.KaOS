using System;
using System.Runtime.CompilerServices;

namespace EifelMono.KaOS
{
    /*
    public interface IDeveloper
    {
        event Action<Exception> OnException;

        void Exception(Exception ex);

        event Action<string, string, int, string> OnNotImplemented;

        void NotImplemented(string message,
                                 [CallerMemberName] string callerMemberName = "",
                                 [CallerLineNumber] int callerLineNumber = -1,
                                 [CallerFilePath] string callerFilePath = "");

        event Action<string, string, int, string> OnNoEquivalentFunctionAvailable;

        void NoEquivalentFunctionAvailable(string message,
                                  [CallerMemberName] string callerMemberName = "",
                                  [CallerLineNumber] int callerLineNumber = -1,
                                  [CallerFilePath] string callerFilePath = "");
    }
    */
}
